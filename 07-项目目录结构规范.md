---
name: 项目目录结构规范
weight: 7
---
# 项目目录结构规范

## 1. 根目录必备文件（所有项目必选）
- README.md、.gitignore、LICENSE、CHANGELOG.md、CONTRIBUTING.md、.editorconfig
- 多环境 .env.dev / .env.test / .env.prod；.env.local 本地配置不提交仓库
- 语言配套校验文件：.eslintrc、prettier、Roslyn配置等
- 后端强制 Dockerfile、docker-compose.yml、CI/CD 流水线文件；前端/桌面可选

## 2. 通用业务目录分层标准
```
project-root/
├─ config/      全局配置、中间件、权限、环境配置
├─ utils/       纯工具函数：日期、加密、转换，无业务逻辑
├─ common/      业务常量、枚举、统一返回体、全局异常
├─ api/         接口封装、拦截器、地址管理
├─ modules/     按业务域拆分核心模块
├─ assets/      静态资源、样式、图标
├─ tests/       单元/集成/E2E测试用例
├─ docs/        需求、架构、数据库、ADR决策文档
└─ scripts/     构建、初始化、部署自动化脚本
```

**区分规则**：utils 纯通用工具；common 业务全局公共定义，禁止混用。

---

## 附录：各技术栈目录模板

### A.1 .NET/Java/Go后端 Clean架构模板
```
project-root/
├─ src/
│  ├─ Domain/          领域实体、值对象、领域事件、自定义异常
│  ├─ Application/     用例编排、DTO、仓储抽象接口
│  ├─ Infrastructure/  数据库、MQ、第三方API实现
│  └─ API/             控制器、HTTP入口、请求响应格式化
├─ tests/              分四层单元测试目录
├─ config/
├─ scripts/            迁移脚本、初始化数据
├─ docs/decisions      ADR文档
├─ Dockerfile
├─ docker-compose.yml
└─ CI流水线配置
```

### A.2 Vue3/React前端通用模板
```
project-root/
├─ src/
│  ├─ api/             接口封装
│  ├─ assets/          静态资源
│  ├─ common/          常量、枚举
│  ├─ components/      全局公共组件
│  ├─ config/          全局配置
│  ├─ modules/         按业务域拆分页面、组件、状态、hooks
│  ├─ router/          路由守卫、权限
│  ├─ store/           全局状态
│  ├─ utils/           纯工具函数
│  ├─ App              入口组件
│  └─ main.ts          启动文件
├─ tests/              unit + e2e
├─ public/             静态不打包资源
├─ 多环境env文件
└─ vite/tslint配置
```

### A.3 WPF/Electron桌面应用模板
```
project-root/
├─ src/
│  ├─ Core/            纯业务逻辑，无UI依赖
│  ├─ Services/        网络、文件、硬件服务封装
│  ├─ Infrastructure/  本地数据库、文件持久化
│  └─ UI/              MVVM分层 Views/ViewModels/Controls/Converters
├─ tests/              分层单元测试
├─ assets/             程序图标、资源
└─ scripts/            自动化打包powershell/sh脚本
```
桌面应用不强制Docker，必须配套打包发布脚本。

### A.4 Monorepo全栈项目模板
```
project-root/
├─ backend/            后端服务完整目录
├─ frontend/           前端项目完整目录
├─ shared/             前后端共享DTO、常量、类型定义
├─ docs/               项目总文档
└─ docker-compose.yml  一键启动全栈环境
```
