---
name: 项目搭建流程
weight: 8
---
# 项目搭建流程（9步）

## 步骤1：项目立项与前期准备
1. 梳理需求、用户群体、部署环境、迭代周期
2. 技术负责人完成方案评审，确定架构模式、数据库、中间件
3. 提交仓库申请，运维创建Git仓库，配置分支保护、MR评审权限
4. 初始化docs目录：PRD、architecture、database文档框架
5. 编写ADR技术选型决策文档

## 步骤2：本地开发环境初始化
1. 本地JDK/.NET/Node/Python等开发版本与生产环境严格对齐
2. 全局统一工程化、代码格式化、校验工具
3. 克隆仓库，配置全局git用户名、邮箱、提交模板

## 步骤3：项目骨架初始化
1. 使用公司统一模板/官方脚手架，禁止手动零散创建文件
2. 删除脚手架冗余示例代码、无用依赖
3. 按通用目录结构创建完整文件夹层级
4. 初始化全部根目录强制配置文件

## 步骤4：工程化与代码规范统一配置
1. 开启代码静态校验、保存自动格式化，统一缩进换行引号规则
2. 配置commitlint husky，约束Git提交规范，拦截无效提交
3. 拆分dev/test/prod三套环境变量，禁止硬编码参数
4. 配置构建别名、打包压缩、资源分割、缓存优化
5. 统一.vscode工作区配置，团队开发环境一致

## 步骤5：企业级基础能力封装（优先于业务开发）
1. 统一请求封装：拦截器、超时、错误处理、防重复请求
2. 全局统一异常捕获、标准化错误返回体、自定义领域异常
3. 通用工具封装：脱敏、文件、加密、日期、防抖节流
4. 权限底层封装：登录、路由/接口鉴权、角色判断
5. 标准化分级日志，结构化日志输出，禁止裸字符串打印

## 步骤6：数据库与中间件初始化
1. 按数据库设计文档建表、创建索引、字典表
2. 强制启用数据库迁移工具 EF/Flyway/Alembic，禁止手动执行DDL
3. 配置连接池、读写分离、超时、重连、熔断策略
4. 编写初始化基础数据、角色权限字典脚本

## 步骤7：容器化与CI/CD流水线配置
1. 标准化Dockerfile：基础镜像、资源限制、启动命令
2. docker-compose本地一键启动项目+依赖中间件
3. CI流水线标准流程：拉取代码→依赖恢复→格式校验→编译→单元测试→覆盖率→SAST安全扫描→构建产物
4. 质量门禁：编译0错误、无警告（不接受注解忽略警告）、测试全量通过、覆盖率达标、无高危漏洞
5. 多环境流水线隔离，开发/测试/生产互不干扰

## 步骤8：项目全维度校验
搭建完成必须全部校验通过才能开发业务：
- 规范性：目录、命名、配置符合规范
- 功能性：正常启动、环境切换、接口连通正常
- 稳定性：重启、切换环境、重复构建无报错、无端口冲突
- 安全性：排查密钥硬编码、依赖漏洞、权限绕过风险
- 自动化：本地打包、流水线构建、镜像部署全流程正常

## 步骤9：代码归档初始化
1. 完善README、部署、接口全套文档
2. 首笔提交遵循Conventional Commits规范
3. 合并初始化骨架代码至dev分支，锁定基础架构
4. 同步项目信息至公司项目台账
