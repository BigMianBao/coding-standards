---
name: 代码命名规范
weight: 9
---
# 代码命名规范

## 1. 通用命名规则

| 类型 | 命名风格 | 示例 |
|------|----------|------|
| 类/接口 | PascalCase | `UserService`、`IOrderRepository` |
| 接口前缀 | I + PascalCase | `IUserRepository` |
| 方法 | PascalCase | `GetUserById` |
| 属性 | PascalCase | `UserName` |
| 私有字段 | _ + camelCase | `_dbContext` |
| 局部变量/方法入参 | camelCase | `userId`、`orderList` |
| 常量 | PascalCase | `MaxPageSize` |

## 2. 禁止项
- 禁止无意义单字母变量（仅循环 i/j 允许）
- 禁止拼音命名
- 禁止缩写歧义（如 `usr`、`mgr` 等非通用缩写）
