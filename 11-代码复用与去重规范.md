---
name: 代码复用与去重规范
weight: 11
---
# 代码复用与去重规范

## 1. 核心原则
- 同一逻辑只允许存在一份实现，禁止相同或相似代码散落在不同文件/模块中
- 发现重复必须抽取，不允许"先复制后面再改"
- 抽取位置由使用范围决定，不能随意放置

## 2. 重复判定标准

| 重复类型 | 判定条件 | 处理方式 |
|----------|----------|----------|
| 完全重复 | 代码逻辑完全一致，仅变量名不同 | 必须立即抽取 |
| 结构重复 | 代码结构相同，仅参数/条件值不同 | 必须抽取，通过参数化消除差异 |
| 逻辑重复 | 不同代码实现相同业务意图 | 应当抽取为统一方法，消除歧义实现 |

### 量化阈值
- **2处重复**：建议抽取，如果逻辑复杂或变更频率高则必须抽取
- **3处及以上重复**：强制抽取，Code Review 不通过
- **跨模块重复**：无论几处，必须抽取

## 3. 抽取位置决策

```
重复代码出现在哪里？              抽取到哪里？
─────────────────────────────────────────────────
同一文件内多处            ──▶   当前文件内提取私有方法
同一模块内不同文件         ──▶   当前模块内的公共文件
同业务域不同模块           ──▶   业务域的公共层（如 modules/shared/）
跨业务域、3个以上模块使用   ──▶   utils/（纯工具）或 common/（业务公共）
前后端都需要               ──▶   shared/（Monorepo共享层）
```

### 禁止行为
- 禁止把业务逻辑抽到 utils/（utils 只能放纯工具函数）
- 禁止把只被 1 个模块用的函数抽到 common/（就近放在模块内部）
- 禁止抽到"万能工具类"里堆砌（单个文件不超过 300 行，按职责拆分文件）

## 4. 常见重复场景与处理

### 4.1 重复的数据转换/格式化
```
❌ 错误：3个页面各自写了一套日期格式化逻辑
✅ 正确：抽取到 utils/date.ts，统一调用 formatDate()
```

### 4.2 重复的接口调用逻辑
```
❌ 错误：多个组件各自调用 getUserInfo 并处理 loading/error 状态
✅ 正确：封装为 useUserInfo hook（前端）或 UserService（后端），统一复用
```

### 4.3 重复的校验逻辑
```
❌ 错误：注册页和编辑页各自写了一套手机号校验正则
✅ 正确：抽取到 common/validators.ts，统一调用 validatePhone()
```

### 4.4 重复的 CRUD 模式
```
❌ 错误：每个模块手写增删改查的 controller + service 模板代码
✅ 正确：抽取通用 BaseCrudService / useCrudTable hook，各模块继承或组合
```

### 4.5 重复的错误处理/Loading 状态
```
❌ 错误：每个接口调用都手写 try/catch + loading 切换
✅ 正确：封装统一请求拦截器自动处理，或使用 useRequest/useMutation 封装
```

## 5. 抽取规范

### 5.1 命名要求
- 抽取后的方法名必须语义清晰，禁止 `handleData()`、`process()` 等泛化命名
- 如果抽取后反而降低可读性（逻辑仅 2 行且只出现 2 次），可以暂不抽取，但需注释标注 `// TODO: 第3处出现时必须抽取`

### 5.2 参数设计
- 参数不超过 4 个，超过则封装为 options 对象
- 禁止用布尔参数控制两种行为（`doSomething(flag)`），应拆为两个语义明确的方法

### 5.3 禁止过度抽象
- 不为"未来可能复用"提前抽取，只处理当前已存在的重复
- 抽象层级不超过 3 层（调用链 A → B → C → D 算过度抽象）
- 如果抽取后调用方需要传入 5 个以上参数才能复用，说明抽取粒度不对，需要重新设计

## 6. Code Review 检查清单

- [ ] 本次变更是否引入了与现有代码重复的逻辑
- [ ] 新增方法是否与已有方法功能重叠（搜关键词确认）
- [ ] 重复代码是否已抽取到正确位置（按使用范围判定）
- [ ] 抽取的公共方法是否有清晰的语义化命名
- [ ] 是否存在"万能工具类"堆积问题（单文件 > 300 行）
- [ ] 是否存在过度抽象（调用链过深、参数过多）
