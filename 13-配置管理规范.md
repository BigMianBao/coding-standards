---
name: 配置管理规范
weight: 11
---
# 配置管理规范

## 1. 多环境配置

| 环境 | 配置文件 | 说明 |
|------|----------|------|
| 开发环境 | .env.dev + .env.local（本地覆盖） | .env.local 不提交仓库 |
| 测试环境 | CI环境变量 + test配置文件 | 由CI注入 |
| 生产环境 | 专用密钥管理服务 | 禁止配置文件存放密钥 |

## 2. 强制要求
- 强类型 Options 绑定配置，禁止直接读取原始配置字符串
- 禁止硬编码 IP、端口、密钥、超时时间、分页大小等魔法数字
- 所有可变参数必须通过配置文件或环境变量注入
