---
name: 安全编码规范
weight: 13
---
# 安全编码规范

## 1. 输入校验
- 所有前端/URL/请求体/Header/文件上传输入强制校验，不信任客户端任何数据
- 后端使用框架校验注解，前端使用 Zod/VeeValidate 做表单校验

## 2. 注入防护
- 禁止字符串拼接SQL，统一参数化查询、ORM框架

## 3. XSS & CSRF
- 前端输出DOM数据自动转义，后端返回HTML进行编码
- 表单接口强制携带CSRF Token
- Cookie配置 HttpOnly、Secure、SameSite

## 4. CORS
- 生产环境禁止 `*` 通配符，配置明确允许域名白名单

## 5. 越权防护
- 接口双层校验：路由鉴权（纵向）+ 资源归属校验（横向）
- 删除、权限变更、资金操作等敏感接口增加二次确认逻辑

## 6. 依赖安全
- CI流水线自动扫描依赖漏洞
- 高危漏洞24小时修复，中危一周内修复
- 禁止引入存在公开CVE未修复的第三方包
